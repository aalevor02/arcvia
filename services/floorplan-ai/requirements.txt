# Core service — this is all you need for the default heuristic backend.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# Classical CV. headless avoids pulling in GTK/Qt, which a server does not have
# and which turns a 40 MB install into a 400 MB one.
opencv-python-headless==4.10.0.84
numpy==2.2.1

# Trained detector backend. Commented out on purpose: it drags in torch
# (~2.5 GB) and is only needed once you set FLOORPLAN_BACKEND=yolo.
# ultralytics==8.3.55

# Reading what the drawing says about itself: room names, and the printed sizes
# that fix the scale. Optional — without it rooms come back unnamed and the user
# calibrates by hand, which is where this started.
#
# Install with --no-deps. rapidocr depends on `opencv-python`, the full build,
# and pip will happily uninstall the headless one above to satisfy it — pulling
# in the GUI libraries a server does not have and cannot load. Everything it
# actually needs is already here.
#
#     pip install onnxruntime
#     pip install --no-deps rapidocr-onnxruntime
#
onnxruntime>=1.20
# rapidocr-onnxruntime==1.4.4   <- see above; --no-deps only

# Reading the PDF decks people actually send instead of drawings. Optional in
# the same way: /health reports reads_pdf, and the service runs without it.
#
# NOT PyMuPDF. It is dual-licensed AGPL-3.0 or commercial, and AGPL reaches
# NETWORK USE — hosting this as a service with PyMuPDF inside would oblige us to
# publish the whole source to every user. It was the only restrictive dependency
# in the entire stack; everything else is MIT/BSD/Apache/CC0.
#
# pypdfium2 (BSD-3/Apache-2.0) gives embedded images at native resolution, which
# is the trick deck.py relies on. pdfplumber (MIT) gives positioned text WITH
# font size, which is what separates a caption from a footer. See pdfbackend.py.
pypdfium2>=5.0
pdfplumber>=0.11
